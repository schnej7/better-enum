{
  "name": "better-enum",
  "version": "1.0.0",
  "description": "Better Enum for TypeScript/Javascript",
  "types": "./lib/types/index.d.ts",
  "main": "./lib/index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "clean": "del-cli ./lib",
    "build": "npm run clean && tsc -p ./tsconfig.json",
    "prepack": "npm run build",
    "test": "tsx --test ./tests/*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/schnej7/better-enum.git"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "del-cli": "^6.0.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
